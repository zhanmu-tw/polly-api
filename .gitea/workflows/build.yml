name: Build and Push Docker Image

on:
  push:
    branches: [main]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build and Push Docker Image
        env:
          DOCKER_CONFIG: /tmp/.docker
        run: |
          mkdir -p $DOCKER_CONFIG
          echo "${{ secrets.REGISTRY_TOKEN }}" | docker --config $DOCKER_CONFIG login ${{ secrets.REGISTRY_URL }} -u ${{ secrets.REGISTRY_USERNAME }} --password-stdin
          docker build -t ${{ secrets.REGISTRY_URL }}/james/forge-polly-api:latest \
                       -t ${{ secrets.REGISTRY_URL }}/james/forge-polly-api:${{ github.sha }} .
          docker --config $DOCKER_CONFIG push ${{ secrets.REGISTRY_URL }}/james/forge-polly-api:latest
          docker --config $DOCKER_CONFIG push ${{ secrets.REGISTRY_URL }}/james/forge-polly-api:${{ github.sha }}

      - name: Notify (Success)
        if: success()
        run: |
          curl -X POST \
            -H "Content-Type: application/json" \
            -d '{"content": "✅ **Forge Polly API** build successful!\n' \
            ${{ secrets.DISCORD_WEBHOOK_URL }}

      - name: Notify (Failure)
        if: failure()
        run: |
          curl -X POST \
            -H "Content-Type: application/json" \
            -d '{"content": "❌ **Forge Polly API** build failed!\n' \
            ${{ secrets.DISCORD_WEBHOOK_URL }}
